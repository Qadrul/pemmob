org.gradle.jvmargs=-Djava.net.preferIPv4Stack=true -Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
org.gradle.java.home=C:\\Program Files\\Java\\jdk-17.0.1
android.useAndroidX=true
android.enableJetifier=true
# Disable Kotlin incremental compilation to avoid cross-drive cache root issues
kotlin.incremental=false
# Disable Kotlin daemon to avoid cache/daemon related failures on CI/Windows
kotlin.daemon.enabled=false
# Prefer IPv4 for Gradle network requests (helps in some Windows networks)
# (kept in the single `org.gradle.jvmargs` line above)
